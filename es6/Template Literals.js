//Template Literals

function printData(sname, age){
    let toPrint = `   Student's name is ${sname} 
and age is ${age}`;
    console.log(toPrint);
}

let sname = "Azhar";
let age = 30;
printData(sname, age);

